#!/usr/bin/env bash
set -eo pipefail

# Placeholder regression test.
# Replace with a test that fails before the fix and passes after it,
# pinning the exact behavior described in the promoted learning.

exit 0
