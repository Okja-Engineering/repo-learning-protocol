repos:
  - repo: local
    hooks:
      - id: rlp-health
        name: RLP health check
        entry: sh -c 'cd <rlp-dir> && ./scripts/learning_health.sh -c'
        language: system
        pass_filenames: false
      - id: rlp-tests
        name: RLP payload tests
        entry: sh -c 'cd <rlp-dir> && ./tests/test_payload.sh'
        language: system
        pass_filenames: false
