# Learnings inbox

Candidate learnings about this repo. Read by triage ONLY — never agent context. Most lines should die here; that is the system working.

Format: `- YYYY-MM-DD · <link> · <class-slug> · <one sentence>`

Examples:
- 2026-09-07 · #247 · api-error-leak · API handlers return raw DB errors to clients
- 2026-09-07 · claude-6f3a9 · jwt-expiry · Middleware does not reject expired JWT tokens

Capture manually, or use `scripts/capture_learnings.sh`:

```bash
scripts/capture_learnings.sh -l "#247" -s "api-error-leak" -t "API handlers return raw DB errors to clients"
```

Field reports welcome: teams using the skill file gaps the same way (link = your repo/issue/PR).

## Auto-captured (untriaged)
